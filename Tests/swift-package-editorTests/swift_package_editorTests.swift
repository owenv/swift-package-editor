import XCTest
@testable import swift_package_editor

final class swift_package_editorTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(swift_package_editor().text, "Hello, World!")
    }
}
